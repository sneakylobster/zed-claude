id = "claude-theme"
name = "Claude"
version = "0.1.0"
schema_version = 1
authors = ["sneakylobster"]
description = "A Claude inspired theme using official Claude colours."
repository = "https://github.com/sneakylobster/zed-claude.git"

[themes]
themes = ["themes/claude.json"]
